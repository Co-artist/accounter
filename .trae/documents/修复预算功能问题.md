## 修复预算功能问题

### 1. 问题分析

**问题1：重置所有预算不可用**
- 代码中存在`resetAllBudgets`函数，但可能存在逻辑问题
- 可能与store的update方法实现有关

**问题2：重置单类预算不可用**
- 代码中存在`resetBudget`函数，但可能存在逻辑问题
- 可能与store的update方法实现有关

**问题3：金额输入框双层显示**
- 金额输入框使用了嵌套设计（div包裹input）
- CSS样式可能存在问题，导致显示双层边框

### 2. 修复步骤

**步骤1：检查并修复重置所有预算功能**
- 分析store中budgets.update方法的实现
- 修复resetAllBudgets函数中的逻辑问题
- 确保重置后数据正确保存到store

**步骤2：检查并修复重置单类预算功能**
- 分析resetBudget函数中的逻辑
- 确保单个预算重置后数据正确保存到store

**步骤3：修复金额输入框双层显示问题**
- 检查CSS样式，确保只有外层容器显示边框
- 确保内层input元素透明无边框
- 优化金额输入框的视觉效果

**步骤4：测试所有修复**
- 测试重置所有预算功能
- 测试重置单类预算功能
- 测试添加预算时的金额输入框

### 3. 预期结果
- ✅ 重置所有预算功能正常工作
- ✅ 重置单类预算功能正常工作
- ✅ 金额输入框只显示一层边框
- ✅ 所有功能视觉效果正常

### 4. 关键代码修改

**文件：BudgetPage.vue**
- 修复`resetAllBudgets`函数
- 修复`resetBudget`函数
- 优化金额输入框的CSS样式

通过以上步骤，我们将解决预算功能的所有问题，确保用户能够正常使用重置预算和添加预算功能。