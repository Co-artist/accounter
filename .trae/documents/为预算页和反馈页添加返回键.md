经过对项目页面的排查，以下是各个页面的返回键情况：

### 已有返回键的页面
以下页面**已经包含**返回键，无需修改：
1.  **`PrivacyPage.vue` (隐私设置)**: 包含 `<button class="back-btn">`。
2.  **`PrivacyPolicy.vue` (隐私政策)**: 包含 `<button class="back-btn">`。
3.  **`UserAgreement.vue` (用户协议)**: 包含 `<button class="back-btn">`。

### 缺少返回键的页面
以下页面目前没有返回键，需要添加：
1.  **`BudgetPage.vue` (预算设置)**:
    *   目前是一个普通的 Header，缺少返回按钮。
    *   **计划**：添加 `<button class="back-btn">` 并绑定 `router.back()`。
2.  **`FeedbackPage.vue` (意见反馈)**:
    *   Header 中缺少返回按钮。
    *   **计划**：添加 `<button class="back-btn">` 并绑定 `router.back()`。

### 无需返回键的页面
以下是主导航页面（底部 Tab 页面），通常不需要顶部返回键：
1.  `HomePage.vue` (首页)
2.  `StatisticsPage.vue` (统计)
3.  `CategoryPage.vue` (分类)
4.  `ProfilePage.vue` (我的)

### 计划修改内容
我将修改 `BudgetPage.vue` 和 `FeedbackPage.vue`，在它们的顶部标题栏左侧添加一个标准的返回箭头按钮，点击后返回上一页，样式与隐私设置页面保持一致。

准备就绪，请确认执行。